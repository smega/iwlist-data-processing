# iwlist-data-processing

Manipulation of data from iwlist linux application. The goal is to get the same json output using different tools or programming languages.
Used:
python, awk, bash, grep

WIP: sed
